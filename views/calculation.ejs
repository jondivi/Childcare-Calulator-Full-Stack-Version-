<%- include('partials/header') -%>
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= calculation.eventName %></h2>
      <div class="row justify-content-between">
        <form
          class="col-1"
          action="/calculation/likeCalculation/<%= calculation.id %>?_method=PUT"
          method="POST">
          <button class="btn fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Total Spent: <%= calculation.totalUSD %></h3>
        <%if(calculation.user == user.id){ %>
        <form
          action="/calculation/deleteCalculation/<%= calculation.id %>?_method=DELETE"
          method="POST"
          class="col-3">
          <button class="btn fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <table>
    <thead>
    <tr>
      <tr class="col-3 mt-5">
        <td> </td>
        <!-- <th scope="col" id="name">Event Name</th>
        <th scope="col" class="ingredients">Ingredients</th>
        <th scope="col" class="directions">Directions</th> -->
        </tr>
    </thead>
    <tbody>
        <tr class="col-3 mt-5">
            <td id="eventName"><%= calculation.eventName %></td>
            <td id="weekDay"><%= calculation.dateOfEvent %></td>
            <td id="startTime"><%= calculation.hoursStart %></td>
            <td id="endTime"><%= calculation.hoursEnd %></td>
            <!-- <td id=""><%= calculation.hoursSpent %></td> -->
            <td id="rate"><%= calculation.amountUSD %></td>
            <!-- <td id=""><%= calculation.totalUSD %></td> -->
        </tr>
    </tbody>
</table>
<button id="calculcate">Calculcate</button>
<h2 id="total"></h2>


<!-- Back Button -->
    <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/profile">Back</a>
      <!-- <a class="btn btn-primary" href="/feed">Return to Feed</a> -->
    </div>
  </div>
</div>
<script src="/public/js/main.js"></script>
<!-- <%- include('partials/footer') -%> -->
